import { listingDate } from 'craigslist/helpers/listing-date';
import { module, test } from 'qunit';

module('Unit | Helper | listing date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingDate([42]);
  assert.ok(result);
});
